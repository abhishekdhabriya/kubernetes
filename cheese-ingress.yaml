apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: aspirecloudtech.com
    http:
      paths:
      - path: /
        backend:
          serviceName: stilton
          servicePort: http
